# sql-server-distributed-backup-automation
Automated distributed SQL Server backup solution using PowerShell and batch scripting. Executes remote backup jobs across multiple SQL Server instances, validates connectivity and file availability, runs backup scripts via xp_cmdshell, captures execution logs, and provides a consolidated status report with parallel job execution.
